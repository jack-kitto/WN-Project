-- CreateTable
CREATE TABLE "area1" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "epochTime" INTEGER NOT NULL,
    "humanTime" TEXT NOT NULL
);

-- CreateTable
CREATE TABLE "area2" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "epochTime" INTEGER NOT NULL,
    "humanTime" TEXT NOT NULL
);

-- CreateTable
CREATE TABLE "area3" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "epochTime" INTEGER NOT NULL,
    "humanTime" TEXT NOT NULL
);
